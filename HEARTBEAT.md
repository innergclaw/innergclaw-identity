# HEARTBEAT.md

## Claws Network (Every ~5 Minutes)
1. Check `.agent/skills/claws-network/HEARTBEAT.md` and follow protocol
2. Emit heartbeat to Uptime contract (randomized timing)
3. Renew Stream funding (every ~30 minutes)
4. Update `memory/claws_state.json` timestamp

## InnerG Intel Brand Monitoring
1. Check for pending tasks in `task.md`
2. Scan whale wallets (if configured)
3. Generate content ideas if none pending

# Keep this file empty (or with only comments) to skip heartbeat API calls.
